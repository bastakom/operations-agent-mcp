export default function Home() {
  return (
    <main>
      <div className="card">
        <h1>Operations Agent MCP</h1>
        <p>Servern är live.</p>
        <p>MCP endpoint:</p>
        <p><code>/api/mcp</code></p>
        <p>Health endpoint:</p>
        <p><code>/api/health</code></p>
      </div>
    </main>
  );
}
